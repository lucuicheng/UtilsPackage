/**
 * {{alias}}列表
 */
unprotectedRouter.get('/api/list/{{ name }}', async (ctx: BaseContext, next) => {
    const body = await {{ name }}Service.list(ctx.request.query);
    ctx.body = {code: 200, body};
});
/**
 * {{alias}}详情
 */
unprotectedRouter.get('/api/{{ name }}/:id', async (ctx: BaseContext, next) => {
    const body = await {{ name }}Service.getSingle(ctx.params.id, ctx.request.body);
    ctx.body = {code: 200, body};
});

/*----用户{{alias}} start----*/
/**
 * 用户创建{{alias}}
 */
unprotectedRouter.post('/api/user/{{ name }}', async (ctx: BaseContext, next) => {
    const body = await userService.{{ name }}Single(ctx.request.body);
    ctx.body = {code: 200, body};
});
/**
 * 用户查询{{alias}}
 */
unprotectedRouter.get('/api/list/user/{{ name }}/:uid', async (ctx: BaseContext, next) => {
    const body = await userService.{{ name }}History(ctx.params.uid, ctx.request.query);
    ctx.body = {code: 200, body};
});
/**
 * 用户更新{{alias}}
 */
unprotectedRouter.put('/api/user/{{ name }}/:uid', async (ctx: BaseContext, next) => {
    const body = await userService.updateSingle{{ name | title }}Record(ctx.params.uid, ctx.request.body);
    ctx.body = {code: 200, body};
});
/**
 * 用户删除{{alias}}
 */
unprotectedRouter.delete('/api/user/{{ name }}/:uid', async (ctx: BaseContext, next) => {
    const body = await userService.deleteSingle{{ name | title }}Record(ctx.params.uid, ctx.request.query);
    ctx.body = {code: 200, body};
});
/*----用户{{alias}} end----*/